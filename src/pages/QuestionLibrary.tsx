import { useEffect,useMemo,useState } from 'react'; import Papa from 'papaparse'; import type { Question } from '../../shared/types'; import { api } from '../api'; import { blankQuestion,QuestionForm } from '../components/QuestionForm';
export function QuestionLibrary(){const [questions,setQuestions]=useState<Question[]>([]),[search,setSearch]=useState(''),[category,setCategory]=useState(''),[subcategory,setSubcategory]=useState(''),[difficulty,setDifficulty]=useState(''),[active,setActive]=useState(''),[usage,setUsage]=useState(''),[editing,setEditing]=useState<Question|null>(null),[error,setError]=useState('');
 const load=()=>api.questions({search,category,subcategory,difficulty,active,usage}).then(setQuestions).catch(e=>setError(e.message)); useEffect(()=>{load()},[search,category,subcategory,difficulty,active,usage]); const categories=useMemo(()=>[...new Set(questions.map(q=>q.category))].sort(),[questions]);const franchises=useMemo(()=>[...new Set(questions.map(q=>q.subcategory).filter(Boolean))].sort(),[questions]);
 const save=async()=>{if(!editing)return;try{const exists=questions.some(q=>q.id===editing.id);await (exists?api.update(editing):api.create(editing));setEditing(null);setError('');load()}catch(e){setError((e as Error).message)}};
 const remove=async(q:Question)=>{if(!confirm(`Delete ${q.id}?`))return;await api.remove(q.id);load()}; const duplicate=async(q:Question)=>{const id=prompt('ID for duplicate',`${q.id}-COPY`);if(id)try{await api.duplicate(q.id,id);load()}catch(e){setError((e as Error).message)}};
 const exportCsv=()=>{const blob=new Blob([Papa.unparse(questions.map(q=>({...q,alternateAnswers:q.alternateAnswers.join('|')})))],{type:'text/csv'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='questions.csv';a.click()};
 const importFile=async(file:File)=>{try{let data:Question[];if(file.name.endsWith('.csv')){const parsed=Papa.parse<Record<string,string>>(await file.text(),{header:true,skipEmptyLines:true});data=parsed.data.map(r=>({...r,difficulty:Number(r.difficulty),pointValue:Number(r.difficulty)*100,alternateAnswers:(r.alternateAnswers??'').split('|').filter(Boolean),active:r.active!=='false',usedCount:Number(r.usedCount??0),questionType:'text',mediaPath:null,dateLastUsed:r.dateLastUsed||null,episodeLastUsed:r.episodeLastUsed||null} as unknown as Question))}else data=JSON.parse(await file.text());await api.importJson(data);setError('');load()}catch(e){setError((e as Error).message)}};const restore=async(file:File)=>{try{await api.restore(JSON.parse(await file.text()));load()}catch(e){setError((e as Error).message)}};
 return <main><header><div><p className="eyebrow">Geek Trivia Live</p><h1>Question Library</h1></div><button onClick={()=>setEditing(blankQuestion())}>New question</button></header>{error&&<div className="error">{error}</div>}<section className="toolbar"><input aria-label="Search questions" placeholder="Search ID, question, or answer…" value={search} onChange={e=>setSearch(e.target.value)}/><select value={category} onChange={e=>setCategory(e.target.value)}><option value="">All categories</option>{categories.map(c=><option key={c}>{c}</option>)}</select><select value={subcategory} onChange={e=>setSubcategory(e.target.value)}><option value="">All franchises</option>{franchises.map(c=><option key={c}>{c}</option>)}</select><select value={difficulty} onChange={e=>setDifficulty(e.target.value)}><option value="">All levels</option>{[1,2,3,4,5].map(x=><option key={x}>{x}</option>)}</select><select value={active} onChange={e=>setActive(e.target.value)}><option value="">Any status</option><option value="true">Active</option><option value="false">Inactive</option></select><select value={usage} onChange={e=>setUsage(e.target.value)}><option value="">Any usage</option><option value="unused">Never used</option><option value="used">Previously used</option></select><label className="file">Import CSV<input type="file" accept=".csv" onChange={e=>e.target.files?.[0]&&importFile(e.target.files[0])}/></label><button className="secondary" onClick={exportCsv}>Export CSV</button><a className="button secondary" href="/api/backup">Backup JSON</a><label className="file secondary">Restore JSON<input type="file" accept=".json" onChange={e=>e.target.files?.[0]&&restore(e.target.files[0])}/></label></section>
 <p>{questions.length} questions</p><div className="table-wrap"><table><thead><tr><th>ID</th><th>Category</th><th>Franchise</th><th>Level</th><th>XP</th><th>Question</th><th>Status</th><th>Uses</th><th></th></tr></thead><tbody>{questions.map(q=><tr key={q.id}><td><code>{q.id}</code></td><td>{q.category}</td><td>{q.subcategory}</td><td>{q.difficulty}</td><td>{q.pointValue}</td><td>{q.questionText}</td><td>{q.active?'Active':'Inactive'}</td><td>{q.usedCount}</td><td className="row-actions"><button className="text" onClick={()=>setEditing({...q})}>Edit</button><button className="text" onClick={()=>duplicate(q)}>Duplicate</button><button className="text danger" onClick={()=>remove(q)}>Delete</button></td></tr>)}</tbody></table></div>{editing&&<div className="modal"><QuestionForm value={editing} onChange={setEditing} onSubmit={save} onCancel={()=>setEditing(null)}/></div>}</main>}
