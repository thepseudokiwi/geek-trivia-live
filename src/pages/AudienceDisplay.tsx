import{useEffect,useState}from'react';import type{PublicLiveState}from'../../shared/types';import{useLiveState}from'../useLiveState';
function useCountdown(state:PublicLiveState|null){const[,tick]=useState(0);useEffect(()=>{if(state?.timer.status!=='running')return;const id=setInterval(()=>tick(x=>x+1),250);return()=>clearInterval(id)},[state?.timer.status,state?.timer.resumedAt]);if(!state)return 0;return state.timer.status==='running'?Math.max(0,state.timer.remainingMs-(Date.now()-Date.parse(state.timer.serverNow))):state.timer.remainingMs}
export function AudienceDisplay({id}:{id:string}){const{state,connected}=useLiveState<PublicLiveState>(id,'audience'),remaining=useCountdown(state);if(!state)return <div className="audience standby"><h1>Geek Trivia Live</h1><p>{connected?'Preparing the show…':'Reconnecting…'}</p></div>;return <div className={`audience mode-${state.mode} ${new URLSearchParams(location.search).get('transparent')==='true'?'transparent':''}`}><header><h1>{state.title}</h1><span className="audience-connection">{connected?'LIVE':'Reconnecting…'}</span></header>{state.mode==='standby'&&<main className="broadcast-center"><p className="eyebrow">Geek Trivia Live</p><h2>Stand by — the show begins soon</h2></main>}{state.mode==='board'&&<main className="audience-board">{state.board.map(c=><section key={c.name}><h2>{c.name}</h2>{c.squares.map(q=><div className={`audience-square state-${q.status} ${q.active?'active':''}`} key={q.key}>{['completed','skipped'].includes(q.status)?<span>{q.status}</span>:<strong>{q.value}</strong>}</div>)}</section>)}</main>}{['question','answer'].includes(state.mode)&&state.question&&<main className="broadcast-question"><p>{state.question.category} · {state.question.value} XP</p><h2>{state.question.text}</h2>{state.mode==='answer'&&state.question.answer&&<div className="answer-reveal"><span>Correct answer</span><strong>{state.question.answer}</strong></div>}<output className={`broadcast-timer ${state.timer.status}`}>{Math.ceil(remaining/1000)}</output></main>}{['scores','final'].includes(state.mode)&&<main className="broadcast-center"><h2>{state.mode==='final'?'Final standings':'Scores'}</h2><ol className="standings">{state.participants.map((p,i)=><li key={p.displayName}><span>{p.placement??i+1}. {p.displayName}</span><strong>{p.score} XP</strong></li>)}</ol></main>}<footer>{state.participants.map(p=><div key={p.displayName}><span>{p.displayName}</span><strong>{p.score}</strong></div>)}</footer></div>}
