import{useState}from'react';import{QuestionLibrary}from'./pages/QuestionLibrary';import{EpisodeBuilder}from'./pages/EpisodeBuilder';export default function App(){const[page,setPage]=useState<'library'|'builder'>('library');return <><nav><strong>Geek Trivia Live</strong><button className={page==='library'?'active':''} onClick={()=>setPage('library')}>Question Library</button><button className={page==='builder'?'active':''} onClick={()=>setPage('builder')}>Episode Builder</button></nav>{page==='library'?<QuestionLibrary/>:<EpisodeBuilder/>}</>}
