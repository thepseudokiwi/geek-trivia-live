import{useEffect,useState}from'react';import{EpisodeBuilder}from'./pages/EpisodeBuilder';import{EpisodeRun}from'./pages/EpisodeRun';import{Episodes}from'./pages/Episodes';import{QuestionLibrary}from'./pages/QuestionLibrary';import{HostConsole}from'./pages/HostConsole';import{AudienceDisplay}from'./pages/AudienceDisplay';import{ContestantPlay}from'./pages/ContestantPlay';
type View={page:'library'|'episodes'|'builder'|'run'|'host'|'audience'|'play';id?:string};
const parse=():View=>{const route=location.hash.replace(/^#/,'').split('?')[0],[page='library',id]=route.split('/');return['library','episodes','builder','run','host','audience','play'].includes(page)?{page:page as View['page'],id}:{page:'library'}};
export default function App(){const[view,setView]=useState<View>(parse);useEffect(()=>{const change=()=>setView(parse());addEventListener('hashchange',change);return()=>removeEventListener('hashchange',change)},[]);const go=(page:View['page'],id?:string)=>{location.hash=`${page}${id?`/${id}`:''}`;setView({page,id})};if(view.page==='play')return <ContestantPlay joinCode={view.id}/>;if(view.page==='audience')return <AudienceDisplay id={view.id!}/>;if(view.page==='host')return <HostConsole id={view.id!} back={()=>go('episodes')}/>;return <><nav><strong>Geek Trivia Live</strong><button className={view.page==='library'?'active':''} onClick={()=>go('library')}>Question Library</button><button className={view.page==='episodes'?'active':''} onClick={()=>go('episodes')}>Episodes</button><button className={view.page==='builder'?'active':''} onClick={()=>go('builder')}>New Episode</button></nav>{view.page==='library'?<QuestionLibrary/>:view.page==='episodes'?<Episodes openBuilder={id=>go('builder',id)} openRun={id=>go('run',id)} openHost={id=>go('host',id)}/>:view.page==='builder'?<EpisodeBuilder episodeId={view.id} back={()=>go('episodes')}/>:<EpisodeRun id={view.id!} back={()=>go('episodes')}/>}</>}
