import{expect,it}from'vitest';import{execFileSync}from'node:child_process';import{DatabaseSync}from'node:sqlite';import fs from'node:fs';import path from'node:path';
it('adds Phase 4C tables to a Phase 4B database without losing data',()=>{const file=path.resolve(`data/test-phase4c-${process.pid}.db`),dir=path.resolve('server/db/migrations'),names=fs.readdirSync(dir).filter(x=>x.endsWith('.sql')).sort(),db=new DatabaseSync(file);db.exec('CREATE TABLE migrations(name TEXT PRIMARY KEY,applied_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)');for(const name of names.slice(0,6)){db.exec(fs.readFileSync(path.join(dir,name),'utf8'));db.prepare('INSERT INTO migrations(name) VALUES(?)').run(name)}db.prepare("INSERT INTO questions(id,category,subcategory,difficulty,point_value,question_text,correct_answer,alternate_answers) VALUES('KEEP','Legacy','',1,100,'Keep','Yes','[]')").run();db.close();execFileSync(process.execPath,['--import','tsx','server/db/migrate.ts'],{cwd:process.cwd(),env:{...process.env,DATABASE_PATH:file}});const upgraded=new DatabaseSync(file);for(const table of['join_sessions','contestant_sessions','buzzer_sessions','buzz_attempts','contestant_statistics'])expect(upgraded.prepare("SELECT 1 FROM sqlite_master WHERE type='table' AND name=?").get(table)).toBeTruthy();expect(upgraded.prepare("SELECT id FROM questions WHERE id='KEEP'").get()).toBeTruthy();upgraded.close();fs.unlinkSync(file)});
